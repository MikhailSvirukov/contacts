# TestTask

`app` — задание 1, Android-приложение для отображения контактов устройства и вызова по номеру из списка.

Собрать `app`:
```
./gradlew :app:build`.
```

`algo` — задание 4, алгоритм выборки сотрудников по юбилейным дням за текущую неделю.

Запуск `algo`:
```
./gradlew :algo:run
```
